version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - varcheck
    - misspell
    - lll
    - paralleltest

formatters:
  enable:
    - gofumpt

run:
  timeout: 2m